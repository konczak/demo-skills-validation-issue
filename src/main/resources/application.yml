spring:
    datasource:
        password: ''
        url: jdbc:h2:mem:test
        username: sa
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
        show-sql: true
